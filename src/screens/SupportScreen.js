import { View, Text, SafeAreaView } from react-native

const SupportScreen = () => {
    return (
        <SafeAreaView>
            <Text>Donations are the only way our developers get money from.</Text>
            <View></View>
        </SafeAreaView>
    )
}

export default SupportScreen